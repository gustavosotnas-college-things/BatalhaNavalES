/**
 * Pacote que faz mediação da entrada, processando-a em dados para o "model" ou "view" (padrão MVC).
 */
package battleship.controller;

//package-info.java é um arquivo para apenas colocar a descrição do pacote no Javadoc.